import React from "react";

import Layout from "../../components/Layout";

const MainPage = () => {
    return (
        <Layout>
            <p>Bienvenido a la web de apuntes</p>
        </Layout>
    );
};

export default MainPage;
